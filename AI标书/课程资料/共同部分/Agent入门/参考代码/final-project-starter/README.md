# Final Project：集成脚手架

这里仅定义最终报告 schema 和 CLI 入口。重新实现并组合前七个 Lesson 的思想，不要直接复制所有作业代码。

先实现 `review` 的 RuleBaseline，使第一组验收测试通过；再接入 Evidence 与 Runtime。CLI 初始返回 TODO 是正常现象。

```powershell
cargo test -p final-project-starter --test acceptance -- --ignored
```
